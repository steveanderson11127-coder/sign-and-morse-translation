"use client";

import { AlertTriangle, AudioLines, Camera, CheckCircle2, Circle, RotateCcw, Sparkles } from "lucide-react";
import { useState } from "react";

type Mode = "sign" | "morse";

export function TranslatorWorkspace({ mode }: { mode: Mode }) {
  const [state, setState] = useState<"empty" | "loading" | "permission">("empty");
  const sign = mode === "sign";
  const title = sign ? "Camera preview" : "Audio signal";
  const icon = sign ? <Camera size={28} /> : <AudioLines size={28} />;
  function begin() { setState("loading"); window.setTimeout(() => setState("permission"), 750); }
  return <div className="grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><section className="overflow-hidden rounded-2xl border border-white/10 bg-[#0b1b30] shadow-2xl shadow-black/20"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div><p className="text-sm font-semibold text-white">{title}</p><p className="mt-1 text-xs text-slate-400">Nothing is being captured yet.</p></div><span className="flex items-center gap-2 text-[11px] font-semibold uppercase tracking-wider text-slate-400"><Circle size={9} /> Offline</span></div><div className="grid min-h-80 place-items-center bg-[radial-gradient(circle_at_50%_0%,#12355a,transparent_55%),linear-gradient(135deg,#0b2038,#07111f)] p-6 text-center">{state === "empty" && <div className="max-w-sm"><div className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-200">{icon}</div><h2 className="text-xl font-semibold">Ready when you are</h2><p className="mt-3 text-sm leading-6 text-slate-400">{sign ? "Start a private camera preview to prepare for supported-sign recognition." : "Start a private audio preview to prepare for Morse signal decoding."}</p><button type="button" onClick={begin} className="mt-6 rounded-full bg-cyan-300 px-5 py-3 text-sm font-bold text-slate-950 transition hover:bg-cyan-200">Start preview</button></div>}{state === "loading" && <div className="max-w-sm"><div className="mx-auto mb-5 h-10 w-10 animate-spin rounded-full border-2 border-cyan-200/20 border-t-cyan-200" /><h2 className="text-xl font-semibold">Preparing your preview</h2><p className="mt-3 text-sm text-slate-400">Checking browser support…</p></div>}{state === "permission" && <div className="max-w-sm"><AlertTriangle className="mx-auto mb-5 text-amber-300" size={34} /><h2 className="text-xl font-semibold">Permission needed</h2><p className="mt-3 text-sm leading-6 text-slate-400">The real {sign ? "camera" : "microphone"} connection will be added in the next build. This screen demonstrates the permission state without collecting data.</p><button type="button" onClick={() => setState("empty")} className="mt-6 inline-flex items-center gap-2 rounded-full border border-white/20 px-5 py-3 text-sm font-semibold hover:bg-white/5"><RotateCcw size={15} /> Reset preview</button></div>}</div></section><aside className="rounded-2xl border border-white/10 bg-[#0b1b30] p-5"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-cyan-200"><Sparkles size={14} /> Live caption</div><div className="mt-5 rounded-xl border border-dashed border-white/15 bg-white/[.025] p-5"><p className="text-lg font-semibold text-slate-300">Your {sign ? "signed words" : "decoded message"} will appear here.</p><p className="mt-3 text-sm leading-6 text-slate-500">We will show a confidence score and ask you to repeat unclear input.</p></div><div className="mt-6 space-y-4 border-t border-white/10 pt-5 text-sm"><div className="flex justify-between"><span className="text-slate-400">Recognition confidence</span><span className="text-slate-300">—</span></div><div className="h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full w-0 rounded-full bg-cyan-300" /></div><div className="flex gap-3 rounded-xl bg-cyan-300/5 p-3 text-xs leading-5 text-slate-300"><CheckCircle2 className="mt-0.5 shrink-0 text-cyan-200" size={15} /> This prototype does not send video or audio anywhere.</div></div></aside></div>;
}
